{
    "name": "collage-sync-api",
    "version": "1.0.0",
    "description": "Cloudflare Workers API for collage cloud sync",
    "main": "src/index.js",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}